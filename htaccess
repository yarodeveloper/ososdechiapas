<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # REDIRIGIR API AL PUERTO 30001
  RewriteRule ^api/(.*) http://localhost:30001/api/$1 [P,L]

  # MANEJAR RUTAS DE REACT (PWA)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
